# Login Left-Side Image

The login page currently references:

- `assets/images/patient-safety-logo.svg`

If you have a licensed Shutterstock image, replace this file (or update the image path in `src/app/features/auth/login.page.ts`) with your downloaded asset.

Current usage in template:

```html
<img src="assets/images/abc.svg" alt="Patient safety logo" />
```

